{
  "name": happy_bank_infra,
  "release": {
    "branches": [
      "main",
      "+([0-9a-zA-Z])?"
    ],
    "tagFormat": "${version}"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}