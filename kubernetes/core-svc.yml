apiVersion: v1
kind: Service
metadata:
  name: core-app-svc  
  labels:
    app: core-app
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: core-app